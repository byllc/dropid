require "bundler/gem_tasks"
require 'prake'

Prake::Application.init

#task_names = Rake.application.top_level_tasks
#task_names.each do |name|
#  Rake.application[name].enhance { puts "clear pid" }
#end